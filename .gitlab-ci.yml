image: docker:latest

services:
- docker:dind

stages:
  - build
  - test

test:
  stage: test

  image: sharpbot-facebook:latest

  entrypoint:
    - python -m flake8


build-master:
  stage: build

  script:
    - docker build --rm -f "Dockerfile" -t sharpbot-facebook:latest .
